# Applied automatically to repos with a personal remote. See the includeIf rules
# in ~/.gitconfig.
#
# The matching key is served by the Bitwarden ssh-agent rather than living on
# disk. Nothing to configure here for it: ssh offers the agent's keys and the
# forge picks the one it recognises.
[user]
	name = nedsi
	email = nedsi@pm.me
